import 'package:flutter/material.dart';

class PCustomCurvedEdges extends StatelessWidget {
  const PCustomCurvedEdges({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}